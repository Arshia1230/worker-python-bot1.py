# -*- coding: utf-8 -*-
"""
تنظیمات ربات مار و پله.

قبل از اجرا حتماً BOT_TOKEN و ADMIN_IDS را پر کنید.
می‌توانید این مقادیر را از طریق متغیرهای محیطی هم بدهید (مناسب برای Render).
"""

import os

# ────────────────────────────────────────────────────────────
# توکن ربات را از @BotFather بگیرید و اینجا قرار دهید
# یا در Render متغیر محیطی BOT_TOKEN را تنظیم کنید.
# ────────────────────────────────────────────────────────────
BOT_TOKEN = os.environ.get("BOT_TOKEN", "اینجا_توکن_ربات_را_بگذارید")

# آیدی عددی ادمین‌ها. با ربات @userinfobot آیدی خود را پیدا کنید.
# می‌توانید چند ادمین بگذارید: [11111111, 22222222]
# یا در Render متغیر ADMIN_IDS را به‌صورت «11111111,22222222» بدهید.
_admins_env = os.environ.get("ADMIN_IDS", "")
if _admins_env.strip():
    ADMIN_IDS = [int(x) for x in _admins_env.replace(" ", "").split(",") if x]
else:
    ADMIN_IDS = [
        123456789,  # ← این عدد را با آیدی عددی خودتان عوض کنید
    ]

# ────────────────────────────────────────────────────────────
# عضویت اجباری در کانال (Force Join)
# کاربر تا در این کانال عضو نشود نمی‌تواند بازی کند.
# برای غیرفعال‌کردن، REQUIRED_CHANNEL را خالی بگذارید: ""
# نکته مهم: ربات باید در این کانال «ادمین» باشد تا بتواند عضویت را بررسی کند.
# ────────────────────────────────────────────────────────────
REQUIRED_CHANNEL = os.environ.get("REQUIRED_CHANNEL", "@nii_thv")
REQUIRED_CHANNEL_URL = os.environ.get("REQUIRED_CHANNEL_URL", "https://t.me/nii_thv")

# مسیر فایل دیتابیس (به‌صورت خودکار ساخته می‌شود)
DB_PATH = os.environ.get("DB_PATH", "snake_ladder.db")

# ────────────────────────────────────────────────────────────
# سه سطح بازی.
# تنها تفاوت سطح‌ها «اندازهٔ زمین» و «تعداد خانه‌ها»
# (و به‌تناسب تعداد مار و نردبان) است؛ قوانین یکسان‌اند.
# ────────────────────────────────────────────────────────────
DIFFICULTIES = {
    "easy": {
        "title": "آسان",
        "cols": 6,
        "rows": 6,        # ۳۶ خانه
        "snakes": 2,
        "ladders": 3,
    },
    "medium": {
        "title": "متوسط",
        "cols": 10,
        "rows": 10,       # ۱۰۰ خانه
        "snakes": 6,
        "ladders": 7,
    },
    "professional": {
        "title": "حرفه‌ای",
        "cols": 12,
        "rows": 12,       # ۱۴۴ خانه
        "snakes": 11,
        "ladders": 12,
    },
}

# حداقل و حداکثر تعداد بازیکن در بازی گروهی
MIN_PLAYERS = 2
MAX_PLAYERS = 6

# اگر True باشد، هرکس ۶ بیاورد یک‌بار دیگر تاس می‌اندازد
ROLL_AGAIN_ON_SIX = True
